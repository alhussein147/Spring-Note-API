spring:
  application:
    name: NotesAPI
  security:
    user:
      password: test
  data:
    mongodb:
      uri: ${MONGO_DB_CONNECTION_STRING}

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET_BASE64}


logging:
  level:
    root: INFO
    com.hussein.notes_api: DEBUG